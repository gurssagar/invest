import SvelteFlickingPanel from "./SvelteFlickingPanel";
declare class PanelManager {
    panels: Record<string, any>;
    dirty: boolean;
    constructor();
    add(panel: SvelteFlickingPanel): void;
    remove(id: string): void;
    get(id: string): any;
}
export default PanelManager;
