import Component from "@egjs/component";
import { ExternalRenderer, PanelOptions, RendererOptions } from "@egjs/flicking";
import SvelteFlickingPanel from "./SvelteFlickingPanel";
export interface SvelteRendererOptions extends RendererOptions {
    getSlots(children: HTMLElement[]): SvelteFlickingPanel[];
    renderEmitter: Component<{
        render: void;
    }>;
    forceUpdate(): void;
}
declare class SvelteRenderer extends ExternalRenderer {
    private _getSlots;
    private _renderEmitter;
    private _forceUpdate;
    constructor(options: SvelteRendererOptions);
    render(): Promise<void>;
    forceRenderAllPanels(): Promise<void>;
    protected _collectPanels(): void;
    protected _createPanel(externalComponent: SvelteFlickingPanel, options: PanelOptions): import("@egjs/flicking/declaration/core/panel/Panel").default;
    private _applyPanelOrder;
}
export default SvelteRenderer;
